//console.log("Gatang dick head");

interface Person {
  name: string;
  age: number;
  cute: boolean;
}

const person: Person = {
  name: "Gear",
  age: 18,
  cute: true,
};

console.log(person.name);
console.log(person.age);
console.log(person.cute);
